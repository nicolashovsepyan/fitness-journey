# docs/ — my working notes

**Not for you.** These are long technical write-ups I keep for myself so I do
not re-learn the same thing twice. They are honest, they are dense, and they
are not written for a reader.

If you want to know how the system fits together, read `SYSTEM.md` at the top
level instead — that one is written to be read.

| | |
|---|---|
| `AUDIT.md` | a file-by-file audit of the whole project |
| `BACKEND.md` | why the data is stored the way it is, and what replaces it |
| `DATABASE.md` | the shape of the movement database |
