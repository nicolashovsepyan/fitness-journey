# The exercise database moved

It now lives in one folder, with the workbook, the tools and the visual dashboard:

    EXERCISE DATABASE/
      README.md              start here
      DASHBOARD.html         the visual view
      SCHEMA-AND-AUDIT.md    the audit + the proposed schema
      workbook/EXERCISES.xlsx  the database itself

Nothing about movements should be kept anywhere else. Two copies of one truth is
the disease this rebuild exists to cure.
