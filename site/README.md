# site/ — a SECOND copy of the app, for a different web address

**This is not the app you use.** Your app is the one at the top level of this
project, published at

    https://nicolashovsepyan.github.io/fitness-journey/

`site/` is an older, separate copy that was set up to publish to **Netlify**,
a different hosting service, from its own folder. It has its own settings file
(`netlify.toml`).

**It is out of date and incomplete.** It was last updated on 17 August, it is
missing `js/` and `styles.css` entirely, and its `index.html` is a small
landing page rather than the app. It almost certainly does not work properly
any more.

## What needs deciding

Nobody has confirmed whether that Netlify address is still in use.

- **If you do not use it** — say so and the whole folder moves to `ARCHIVE/`.
- **If you do use it** — say so and I will bring it back in step with the real
  app and set up a way to keep it there, instead of it drifting.

Until then it is left exactly as it is. It has been taken out of the phone
app's offline download, which it should never have been part of; that change
does not affect the Netlify address at all.
